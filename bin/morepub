#!/usr/bin/perl

use strict;
use warnings;
use FindBin '$Bin';
use lib "$Bin/../lib";
use App::morepub::Epub;

open(my $less, '|-', 'less' )
	or die "Can't open less: $!\n";

App::morepub::Epub->new( file => $ARGV[0])->render_book($less);

exit 0;
